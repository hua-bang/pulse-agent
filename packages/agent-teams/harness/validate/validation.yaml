version: 1

pathRules:
  - name: default
    paths:
      - src/**
      - package.json
      - tsconfig.json
      - tsup.config.ts
    required:
      - pnpm --filter pulse-coder-agent-teams test
      - pnpm --filter pulse-coder-agent-teams typecheck
