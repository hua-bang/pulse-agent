{
  "name": "@pulse-coder/canvas-cli",
  "version": "0.0.1-alpha.1",
  "description": "CLI for Pulse Canvas — agent communication channel for canvas workspaces",
  "type": "commonjs",
  "main": "./dist/index.cjs",
  "bin": {
    "pulse-canvas": "./dist/index.cjs"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs"
    },
    "./core": {
      "types": "./dist/core/index.d.ts",
      "import": "./dist/core/index.cjs",
      "require": "./dist/core/index.cjs"
    }
  },
  "scripts": {
    "build": "tsup && node scripts/prepare-native.mjs && cp -r skills dist/",
    "dev": "tsup --watch",
    "test": "vitest run --no-file-parallelism",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist",
    "skills"
  ],
  "dependencies": {
    "@pulse-coder/storage": "workspace:*",
    "better-sqlite3": "12.11.1",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
