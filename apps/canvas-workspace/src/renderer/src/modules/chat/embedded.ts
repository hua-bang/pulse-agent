// Scheduled tasks render a fixed-scope conversation without the full ChatPage
// router. Keep this heavy visual surface off the broker-focused root entry.
export { ChatPageBody, type ChatPageBodyProps } from './components/ChatPageBody';
